import { Container, Typography } from "@mui/material";

export function HomeSection() {
  return (
    <Container sx={{ height: "100vh" }} maxWidth="lg">
      <Typography variant="h3">HomeSection</Typography>
    </Container>
  );
}
